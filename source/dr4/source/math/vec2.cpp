#include <algorithm>

#include "dr4/math/vec2.hpp"

// dr4::Vec2f dr4::Vec2f::Clamped(dr4::Vec2f min, dr4::Vec2f max) const
// {
//     return {std::clamp(this->x, min.x, max.x), std::clamp(this->y, min.y, max.y)};
// }